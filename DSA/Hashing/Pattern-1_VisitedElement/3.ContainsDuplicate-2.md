    func containsNearbyDuplicate(_ nums: [Int], _ k: Int) -> Bool {
        var dict = [Int:Int]()
        for (ind,num) in nums.enumerated() {
            if let duplicateInd = dict[num], ind - duplicateInd <= k {
                return true
            }
            dict[num] = ind
        }
        return false
}
